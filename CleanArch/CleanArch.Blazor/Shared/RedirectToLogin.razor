@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        Navigation.NavigateTo("Identity/Pages/Account/Login?returnUrl=" + Uri.EscapeDataString(Navigation.Uri), true);
    }
}
